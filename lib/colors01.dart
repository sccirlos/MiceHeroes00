import 'package:flutter/material.dart';

// defining colors by name
const appBlack = Colors.black;
const appGrey = Colors.grey;
const appWhite = Colors.white;

// pull off colors off internet using hex code, create a color widget, FF is full color no opacity- there is a op chart.
const appBackgroundDark = Color(0xFF616161);
const appBackgroundLight = Color(0xFFF5F5F5);

// good to include the above colors in all apps


// other colors pulled from internet
// create another color widget
// change colors lol
const appBluePrimary = Color(0xFF0d47a1);
const appBlueLight = Color(0xFF5472d3);
const appBlueDark = Color(0xFF002171);

//
const appYellowPrimary = Color(0xFFffee58);
const appYellowLight = Color(0xFFffff8b);
const appYellowDark = Color(0xFFc9bc1f);









